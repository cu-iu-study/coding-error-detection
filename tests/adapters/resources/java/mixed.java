public class Test {
    void test() {
        String password = "secret123";

        String token = password;

        String secret = token.strip();

        eval(code);

        os.system(command);

        run(
            "abc",
            token,
            token.strip()
        );
    }
}