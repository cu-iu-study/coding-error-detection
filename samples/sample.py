password = "secret123"

token = api_token

print("Hello World")

eval(code)

os.system(command)

subprocess.run(command)

exec(script)

os.system(command.strip())

password = prefix + secret

eval(password="TopSecret")
