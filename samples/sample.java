String password = "secret123";

String token = apiToken;

System.out.println("Hello World");

engine.eval(code);

runtime.exec(command);

process.run(command);

executor.execute(script);

Runtime.getRuntime().exec(command.trim());

String password = user + secret;