const password = "secret123";

const token = apiToken;

console.log("Hello World");

eval(code);

system(command);

run(command);

execute(script);

exec(command.trim());

const password = prefix + secret;